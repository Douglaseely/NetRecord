using NetRecord.Utils;

namespace NetRecord.Services;

public class NetRecordFactory : IHttpClientFactory
{
    public HttpClient CreateClient(string name)
    {
        throw new NotImplementedException();
    }
}